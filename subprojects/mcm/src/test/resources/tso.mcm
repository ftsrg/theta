stores = for_each_thread begin
            mo union po
         end

coherence = for_each_thread begin
                for_each_node target(mo(source(rf(A -> R[thrd])) --> W)) begin
                    rf intersect (W[node] * R[thrd])
                end
            end


stores acyclic
coherence acyclic