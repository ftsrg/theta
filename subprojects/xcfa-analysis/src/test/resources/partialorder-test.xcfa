var x : int

// process1:
// switch:
// [] b: if (x == 1) { E: x = 2; }
// [] d: (nothing)

// process2:
// C: x = 1; assert(x!=2);

// the program has to fail

main process process1 {

	main procedure mainProcedure(){
		init loc L0
		loc L1
		final loc L2
		var y : int

        L0 -> L1 { // b
            assume x = 1
        }
        L1 -> L2 {
            x := 2
        }
        L0 -> L2 { // d
            y := y
        }
	}
}

process process2 {

	main procedure mainProcedure(){
		init loc L0
		loc L1

		final loc L2
		error loc LE

        L0 -> L1 {
            x := 1
        }

        L1 -> L2 {
            assume x = 1
        }

        L1 -> LE {
            assume x = 2
        }
	}
}

