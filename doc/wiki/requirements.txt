# mkdocs 1.6+ is required: the wiki uses build hooks (doc/wiki/hooks.py) and File.generated.
# Pinned below 2.0, which removes the plugin/hook system these rely on.
mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
mkdocs-awesome-pages-plugin>=2.9,<3
python-markdown-math>=0.8
